About String in cpp

name.size() = give the size of the String
ex:
    string str = "Hello"
    str[0] = 'hel'  // This is not valid because the string store only one character at 0 index
                    and also store only one char at every index
    
Note : In cpp we can easily add to string like 
    string str1 = "hello"
    string str2 = "world"

    string result = str1+str2;

Note: we use cin.ignore() for ignore the curser position at current place help to take input
        from next line 
        cin >> t;
        cin.ignore(); // we have to use this otherwise only t-1 line print by the loop
        while(t--){
            string str;
            getline(cin ,  str);
            cout << str<<endl;
        }

### For good practice 
    nameofstring.push_back(str[i]);
    // fgjfgjl